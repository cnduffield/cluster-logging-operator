apiVersion: "logging.openshift.io/v1"
kind: ClusterLogForwarder
metadata:
  name: instance
spec:
  pipelines:
    - name: application-logs
      inputRefs:
        - application
      outputRefs:
        - default
